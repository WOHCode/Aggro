{
  "name": "stacks-blog-feed",
  "version": "1.0.0",
  "description": "Fetches RSS/Atom feeds and merges them into a single JSON file for a static Instagram-style feed reader.",
  "main": "scripts/fetch-feeds.js",
  "scripts": {
    "fetch": "node scripts/fetch-feeds.js"
  },
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.2.0",
    "rss-parser": "^3.13.0"
  }
}
